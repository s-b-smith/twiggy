export enum AppPath {
  Home = '',
  Body = '/body',
  Clothes = '/clothes',
  Color = '/color',
  Background = '/background'
}
// TODO: Look into latest lazy export update
export enum Editor {
  Body,
  Clothes,
  Color,
  Background,
  None
}
